import {
  activeLabel
} from 'ember/helpers/active-label';

module('ActiveLabelHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = activeLabel(42);
  ok(result);
});
