import {
  stateLabel
} from 'ember/helpers/state-label';

module('StateLabelHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = stateLabel(42);
  ok(result);
});
