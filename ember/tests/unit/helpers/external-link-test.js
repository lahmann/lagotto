import {
  externalLink
} from 'ember/helpers/external-link';

module('ExternalLinkHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = externalLink(42);
  ok(result);
});
