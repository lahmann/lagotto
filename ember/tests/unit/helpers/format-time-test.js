import {
  formatTime
} from 'ember/helpers/format-time';

module('FormatTimeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatTime(42);
  ok(result);
});
