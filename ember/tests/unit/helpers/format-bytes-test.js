import {
  formatBytes
} from 'ember/helpers/format-bytes';

module('FormatBytesHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatBytes(42);
  ok(result);
});
