import {
  signPosts
} from 'ember/helpers/sign-posts';

module('SignPostsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = signPosts(42);
  ok(result);
});
