import {
  formatNumber
} from 'ember/helpers/format-number';

module('FormatNumberHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatNumber(42);
  ok(result);
});
